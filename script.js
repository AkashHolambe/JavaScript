console.log("Hello Akash..");
age = 23;
console.log(age);

// Variable which start form number is not allowed and also spacing is also not allowed.

var Variable =
  "This variable have global scope so it will get accessed verywhere.";

// let and cont have local scope

if (true) {
  let a = 20;
}
console.log(a); // so here it give error in consloe so it prove that it dont have access outside the barcket.

const variable =
  "This have fixed value not we cant not change its value this is fixed now";
